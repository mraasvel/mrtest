-- TODO --

- Learn everything that's possible with macro's

	https://gcc.gnu.org/onlinedocs/cpp/Concatenation.html#Concatenation

	- Vector implementation for tests
	- addUniqueVector body implementation
	- Main function to execute all addUniqueIdToVector functions using `dlsym`
	- Main function can be in a header, behind a define `#ifdef MRTEST_MAIN`
	- Executing all functions in the FunctionVector

	- HashTable to run specific functions through arguments
	- HashTable of Vectors: running a "test-suite" instead of single test-cases

- Macro to generate a function

	Concept:

		TEST_CASE("tag_function_name") {
			assertions
		}

		Becomes:

		void __tag_function_name(void) {
			assertions
		}

		Problem: spaces in tag should be replaced with '_'

- Main to execute all generated functions

	HashTable[TagName] = Function;

	for each argument:
		Table[Argument]();
	
	if no arguments:
		for each id:
			iterator();